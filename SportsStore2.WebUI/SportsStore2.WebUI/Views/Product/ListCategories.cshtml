@model SportsStore2.WebUI.Models.CategoryViewModel

@foreach (var categoryName in Model.Categories) {
    @Html.ActionLink(categoryName, "List", "Product", new { category = categoryName }, new { @class = categoryName == Model.ChosenCategory ? "selected" : String.Empty })
}

